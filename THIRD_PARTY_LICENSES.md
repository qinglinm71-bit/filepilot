# Third-party licenses

生产依赖：React / React DOM（MIT）、Zustand（MIT）、pdf-lib（MIT）、JSZip（MIT）、Lucide React（ISC）。

开发依赖：Vite（MIT）、TypeScript（Apache-2.0）、Tailwind CSS（MIT）、Vitest（MIT）、Testing Library（MIT）、Playwright（Apache-2.0）、ESLint（MIT）、Prettier（MIT）、vite-plugin-pwa（MIT）。

版本与传递依赖以 `package-lock.json` 为准。各依赖的完整许可证文本可在对应 npm 包和上游仓库中查看。
